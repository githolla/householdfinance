# Used only by the Vite dev proxy in vite.config.js — never exposed to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: point the planner at your own backend route instead of the dev proxy.
# VITE_ANTHROPIC_URL=https://your-api.example.com/planner
